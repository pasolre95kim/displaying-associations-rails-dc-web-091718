<h1><%= @post.title %></h1>

<!-- Add code here to link to the post's category. -->
<!-- @post.category is Category model itself. so can use it anywhere we
would use @category -->
<h3>Category: <%= link_to @post.category.name,
    category_path(@post.category) if @post.category %></h3>
<!-- added if @post.category conditional to ensure the view doesn't try
to call @post.category.name if the post had not been associated with a category --->



<p><%= @post.description %></p>
